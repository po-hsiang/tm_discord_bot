[project]
name = "tm-discord-bot"
version = "0.2.12"
description = ""
readme = "README.md"
authors = [{ name = "po-hsiang", email = "wsxxcv11733@gmail.com" }]
requires-python = ">=3.14"
dependencies = [
    "discord>=2.3.2,<3",
    "pygsheets>=2.0.6,<3",
    "python-dotenv>=1.0,<2",
    "requests>=2.31,<3",
]

[tool.uv]
# 本專案以 scripts/ 為匯入根目錄直接執行，不作為套件安裝（等同 poetry install --no-root）
package = false
